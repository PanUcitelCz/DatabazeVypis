<Window x:Class="DatabazeVypis.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Studenti" Height="600" Width="900">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="6" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- TABULKA STUDENTŮ -->
        <DataGrid x:Name="StudentsGrid"
                  Grid.Row="0"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  IsReadOnly="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"
                                    Binding="{Binding Id, Mode=OneWay}"
                                    IsReadOnly="True"
                                    Width="70"/>
                <DataGridTextColumn Header="Jméno"
                                    Binding="{Binding FirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    Width="*"/>
                <DataGridTextColumn Header="Příjmení"
                                    Binding="{Binding LastName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    Width="*"/>
                <DataGridTextColumn Header="Ročník"
                                    Binding="{Binding Year, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    Width="90"/>
                <DataGridTextColumn Header="E‑mail"
                                    Binding="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    Width="2*"/>
                <DataGridTextColumn Header="Vytvořeno"
                                    Binding="{Binding CreatedAt, Mode=OneWay, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}"
                                    IsReadOnly="True"
                                    Width="180"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- DĚLIČ -->
        <GridSplitter Grid.Row="1"
                      Height="6"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Center"
                      Background="#DDD"/>

        <!-- FORMULÁŘ PRO PŘIDÁNÍ / OVLÁDÁNÍ -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ID (jen pro informaci; při přidání je prázdné) -->
            <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,6,8" VerticalAlignment="Center" Text="ID:"/>
            <TextBox  x:Name="TxtId" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" IsReadOnly="True"/>

            <!-- Jméno -->
            <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,6,8" VerticalAlignment="Center" Text="Jméno:"/>
            <TextBox  x:Name="TxtFirstName" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>

            <!-- Příjmení -->
            <TextBlock Grid.Row="1" Grid.Column="3" Margin="0,0,6,8" VerticalAlignment="Center" Text="Příjmení:"/>
            <TextBox  x:Name="TxtLastName" Grid.Row="1" Grid.Column="4" Margin="0,0,0,8"/>

            <!-- Ročník -->
            <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,6,8" VerticalAlignment="Center" Text="Ročník (1–6):"/>
            <TextBox  x:Name="TxtYear" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"/>

            <!-- E‑mail -->
            <TextBlock Grid.Row="2" Grid.Column="3" Margin="0,0,6,8" VerticalAlignment="Center" Text="E‑mail:"/>
            <TextBox  x:Name="TxtEmail" Grid.Row="2" Grid.Column="4" Margin="0,0,0,8"/>

            <!-- Ovládací tlačítka -->
            <StackPanel Grid.Row="0" Grid.RowSpan="3" Grid.Column="5" Orientation="Vertical" HorizontalAlignment="Right">
                <Button x:Name="BtnAddStudent"
                        Content="Přidat studenta"
                        Padding="16,6"
                        Margin="0,0,0,8"
                        Click="BtnAddStudent_Click"/>
                <Button x:Name="BtnDeleteSelected"
                        Content="Smazat vybraného"
                        Padding="16,6"
                        Margin="0,0,0,8"
                        Click="BtnDeleteSelected_Click"/>
                <Button x:Name="BtnSave"
                        Content="💾 Uložit data"
                        Padding="16,6"
                        Click="BtnSave_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
