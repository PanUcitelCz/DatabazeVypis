<Window x:Class="DatabazeVipis.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Studenti" Height="600" Width="900">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="6" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- TABULKA STUDENTŮ -->
        <DataGrid x:Name="StudentsGrid"
                  Grid.Row="0"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  IsReadOnly="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="70"/>
                <DataGridTextColumn Header="Jméno" Binding="{Binding FirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Příjmení" Binding="{Binding LastName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Ročník" Binding="{Binding Year, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="90"/>
                <DataGridTextColumn Header="E‑mail" Binding="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="2*"/>
                <DataGridTextColumn Header="Vytvořeno"
                                    Binding="{Binding CreatedAt, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}"
                                    Width="180"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- DĚLIČ -->
        <GridSplitter Grid.Row="1" Height="6" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="#DDD"/>

        <!-- FORMULÁŘ (bez bindingů – čteme v code-behind) -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ID -->
            <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,6,8" VerticalAlignment="Center">ID</TextBlock>
            <TextBox  x:Name="TxtId"
                      Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" IsReadOnly="True"/>

            <!-- Jméno -->
            <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,6,8" VerticalAlignment="Center">Jméno</TextBlock>
            <TextBox  x:Name="TxtFirstName"
                      Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>

            <!-- Příjmení -->
            <TextBlock Grid.Row="1" Grid.Column="3" Margin="0,0,6,8" VerticalAlignment="Center">Příjmení</TextBlock>
            <TextBox  x:Name="TxtLastName"
                      Grid.Row="1" Grid.Column="4" Margin="0,0,0,8"/>

            <!-- Ročník -->
            <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,6,8" VerticalAlignment="Center">Ročník</TextBlock>
            <TextBox  x:Name="TxtYear"
                      Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"/>

            <!-- E-mail -->
            <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,6,0" VerticalAlignment="Center">E‑mail</TextBlock>
            <TextBox  x:Name="TxtEmail"
                      Grid.Row="3" Grid.Column="1" Margin="0,0,0,0"/>

            <!-- TLAČÍTKA -->
            <StackPanel Grid.Row="0" Grid.RowSpan="4" Grid.Column="5" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" >
                <Button x:Name="BtnAdd" Content="Přidat" Padding="16,6" Margin="0,0,8,0" Click="BtnAdd_Click"/>
                <Button x:Name="BtnDelete" Content="Smazat" Padding="16,6" Margin="0,0,8,0" Click="BtnDelete_Click"/>
                <Button x:Name="BtnSave" Content="💾 Uložit" Padding="16,6" Click="BtnSave_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
